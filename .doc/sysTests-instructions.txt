step 1:
---------------

download chromesdriver.exe from http://chromedriver.storage.googleapis.com/index.html
copy it to c:/utilities/chromesdriver
add "c:/utilities/chromesdriver" to system path in window Environment


step 2:
---------------

install the following npm under test=folder in your project.

    npm install selenium-webdriver
    npm install async

step 3:
-----------

install mocha globally

    npm install -g mocha

